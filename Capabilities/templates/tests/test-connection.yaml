apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Capabilities.fullname" . }}-test-connection"
  labels:
{{ include "Capabilities.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "Capabilities.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
